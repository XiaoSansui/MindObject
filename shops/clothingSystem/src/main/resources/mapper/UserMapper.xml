<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dc.dao.CommodityInformationDao">
    <!--用户登陆-->
    <select id="selectloginuser" resultType="com.dc.pojo.User">
        select * from user where is_deleteitems=1 and user_id=#{user_id} ;

    </select>
</mapper>